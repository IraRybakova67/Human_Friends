package service;

import model.Commands;
import model.Description;
import model.Title;
import model.Types;

public class Controller {

    /* private final DataService dataService = new DataService();
    

    public void createTitle(String desccription) {

        dataService.getList(TypeEnum.Title);
        dataService.getIdNext(TypeEnum.Title);

        dataService.create(desccription, TypeEnum.Title); // создать название

    }

    public void createTitle2(String name) {

    }

    public void createCom(String desccription) { // создать команду

        dataService.create(desccription, TypeEnum.Command);
        dataService.getList(TypeEnum.Command);

    }

    public void createTy(String desccription) { // создать тип

        dataService.create(desccription, TypeEnum.Type);
        dataService.getList(TypeEnum.Type);

    }

    public Title titleAnimal(int idTitle) {
        Description title = dataService.getById(TypeEnum.Title, idTitle); // выбрать название животного
        return (Title) title;

    }

    public Types typeAnimal(int idType) {
        Description type = dataService.getById(TypeEnum.Type, idType); // выбрать тип животного

        return (Types) type;

    }

    public Commands commandAnimal(int idCom) {

        Description command = dataService.findById(idCom, (dataService.getList(TypeEnum.Command))); // выбрать команду для животного

        return (Commands) command;
    } */

}
